[GGML 函式庫](<#GGML 函式庫>)支援了多種不同的[量化](#量化)方法。常見的量化類型主要分為兩大類：

- Q?_0/Q?_1 系列（基於全域或向量級取絕對值最大值的簡單均勻量化）
- Q?_K 系列（K-quants，基於子塊多尺度最佳化的分塊量化）。

以 8 位量化為例，最常見的有 Q8_0（最基礎的 8-bit 均勻量化）和 Q8_K（K-quants 的 8-bit 變體），以及在更低位寬下為提升關鍵張量精度而設計的 “XL” 變體（如 q3_k_l），它們在精度、速度和記憶體佔用之間各有所長。